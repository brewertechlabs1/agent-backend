# Style Corpus — the source signal for Richard's voice

Drop raw material here; this is what turns the draft Style Guide in
`../../persona.md` into the real thing.

What to add (as `.json` entries with `"type": "transcript"` or `"phrase"`,
or as plain `.txt`/`.md` alongside):

1. **Transcripts of Richard speaking** — casual and on-topic (calls,
   voice notes, video transcripts).
2. **Richard's writing in his own voice** — posts, captions, messages.
3. **Richard explaining/teaching something** — to capture how he breaks
   things down.

Once a few samples are in, distill from them: signature phrases,
never-says, vocabulary, tone-by-relationship — and replace every
`[draft]` / `[placeholder]` in `persona.md`, marking them confirmed.
