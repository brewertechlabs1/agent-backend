# Secrets — never commit
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Voice clone data — Richard-controlled, stays local
voice-data/

# App users and sessions — credentials, stay local
data/

# Photos (encrypted) and generated video clips
media-data/

# Private brain entries (scope: private lives outside git if desired)
clone/brain/**/*.local.json

# OS / editor
.DS_Store
